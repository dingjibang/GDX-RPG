apply plugin: "groovy"

sourceCompatibility = 1.8
[compileGroovy, compileJava, compileTestJava]*.options*.encoding = 'UTF-8'

sourceSets {
	main {
		java { srcDirs = [] }
		groovy { srcDirs = ["src/"] }
	}
}